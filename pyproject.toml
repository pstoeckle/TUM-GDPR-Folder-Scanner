[tool.poetry]
name = "scan-svn"
version = "0.1.0"
description = ""
authors = ["Patrick Stöckle <patrick.stoeckle@tum.de>"]

[tool.poetry.dependencies]
python = ">=3.9,<4.0"
typer = "^0.4.0"
tika = "^1.24"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
